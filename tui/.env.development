# 开发环境变量（Bun 在 NODE_ENV=development 时自动加载此文件）
#
# 此文件应当提交进版本库：它承载的是「dev 默认行为」，不含任何密钥。
# 个人临时覆盖请写 .env.local（已 gitignore），优先级高于本文件。
#
# 生产二进制不受此文件影响：构建时传了 --no-compile-autoload-dotenv，
# 编译产物不会读取运行目录下的任何 .env（详见 scripts/build.ts）。

# 调试控制台：启动即展开 OpenTUI TerminalConsole 覆盖层，Ctrl+G 切换显隐。
# TUI 接管 stdout 后 console.log 会被渲染帧覆盖，必须走这个覆盖层才看得到。
CCQ_DEBUG=1
